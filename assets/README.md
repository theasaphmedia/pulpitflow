# Assets

Drop static assets here. Currently expected:

- `google_logo.png` — used on the auth screen's "Continue with Google"
  button. The screen falls back to a Material icon if the file is missing,
  so this is optional. Recommended size: 48×48 PNG with transparent
  background.
